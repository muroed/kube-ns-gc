apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-ns-gc.fullname" . }}
  labels:
    {{- include "kube-ns-gc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-ns-gc.selectorLabels" . | nindent 4 }}
